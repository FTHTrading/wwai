import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Sponsor Packages & Pricing — TROPTIONS™",
  description: "Five sponsor tiers from $2,500 to $120,000/month. Local Venue, Category Sponsor, City Activation, World Cup Premium, and Enterprise Brand Partner.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
