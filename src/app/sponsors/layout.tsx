import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Sponsor Partners — TROPTIONS™",
  description: "View and manage all TROPTIONS sponsor partners. Track active campaigns, revenue, proposals, and venue placements per sponsor.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
